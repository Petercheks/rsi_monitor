[project]
name = "rsi-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.2",
    "python-dotenv>=1.0.0",
    "requests>=2.33.1",
]
